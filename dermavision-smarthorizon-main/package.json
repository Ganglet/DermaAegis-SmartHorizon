{
  "name": "dermavision",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n \"API,UI\" -c \"cyan,magenta\" \"python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload\" \"npm run dev --prefix frontend\"",
    "api": "python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload",
    "ui": "npm run dev --prefix frontend",
    "install:frontend": "npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
